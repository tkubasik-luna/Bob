export const WS_URL = "ws://127.0.0.1:8000/ws/chat";

/** Backend HTTP origin — symmetric with {@link WS_URL}. The REST endpoints
 * (`GET /api/llm/...`) live here. */
export const API_BASE_URL = "http://127.0.0.1:8000";
