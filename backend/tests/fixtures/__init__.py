"""Test fixture data + snapshot files.

``tool_calling`` (issue 0057) holds the golden tool-calling fixtures that lock
today's behaviour across the three divergent parse paths; the ``prompts/``
directory holds the context-assembler golden-prompt snapshots (issue 0043).
"""
